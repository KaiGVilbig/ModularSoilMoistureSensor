# Firmware

Sensor node firmware for the Seeed XIAO nRF52840. See root [CLAUDE.md](../CLAUDE.md) for architecture (`Config.h`, `SoilSensor`, `SensorManager`, `BLEHandler`) and the BLE JSON protocol.

Not yet implemented.
